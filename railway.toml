[build]
builder = "nixpacks"

[deploy]
startCommand = "uv run uvicorn src.http_routes:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
